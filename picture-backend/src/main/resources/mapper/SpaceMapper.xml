<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.flipped.picturebackend.mapper.SpaceMapper">

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id,
        spaceName,
        spaceLevel,
        maxSize,
        maxCount,
        totalSize,
        totalCount,
        userId,
        createTime,
        editTime,
        updateTime,
        isDelete
    </sql>

    <!-- 可根据需要添加自定义查询 -->
</mapper>